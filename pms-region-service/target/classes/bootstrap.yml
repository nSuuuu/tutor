spring:
  application:
    name: pms-region-service
  config:
    import: "nacos:"
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: pms
        file-extension: yaml
        shared-configs:
          - data-id: common-config.yml
            group: DEFAULT_GROUP
            refresh: true 